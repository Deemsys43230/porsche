services:
  agent-starter-react:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-starter-react
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env.local
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
